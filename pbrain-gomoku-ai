#!/usr/bin/env python3
"""
Gomoku AI - pbrain-gomoku-ai

This is the main executable for the Gomoku AI brain.
It must be named with the 'pbrain-' prefix for protocol compatibility.

Usage:
    ./pbrain-gomoku-ai [--debug] [--depth N]

The AI communicates via stdin/stdout using the standard Gomoku AI protocol.
"""

import sys
import os

# Add the parent directory to the path so we can import src
script_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, script_dir)

from src.main import main

if __name__ == "__main__":
    sys.exit(main())
